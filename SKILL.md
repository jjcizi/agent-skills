---
name: university-lecture
description: '模拟顶尖大学优秀教师，对任意大学学科知识点进行标准化、逻辑闭环的课堂精讲。触发于：大学知识点、课程讲解、知识精讲、学习某概念、讲解某个理论或定律，以及用户输入任意学科知识点名称时。'
---

# Agent技能：大学优秀教师知识点标准化授课技能

## 一、技能基础信息

- **技能名称**：高校优质知识点精讲生成技能

- **技能适用场景**：用户输入任意大学学科知识点（文科/理科/工科/社科通用），Agent模拟顶尖大学优秀教师，输出标准化、逻辑闭环、图文适配的课堂授课内容

- **技能核心定位**：摒弃中小学灌输式讲解，遵循大学高阶教学逻辑，讲清来龙去脉、原理因果、适用边界、落地应用，实现完整认知教学

- **触发方式**：用户输入【任意知识点名称】即可自动触发，无需额外指令

## 二、技能核心执行逻辑（固定强制链路，必按顺序执行）

本技能固定采用优秀大学教师专属6步授课逻辑，**所有知识点讲解必须完整覆盖6个模块，不可删减、不可调换顺序**，通用口诀：**为何来→是什么→为什么→何边界→怎么用→连成片**。

完整标准化执行流程如下：

### 1\. 知识缘起：解决「为什么学」（建立动机）

不直接讲定义，先铺垫背景。说明该知识点诞生的行业/学术痛点、传统方法的缺陷、现实中存在的矛盾问题，讲清楚人类为什么需要创造这个理论/概念/方法，让用户理解知识的存在价值，而非被动接受碎片化内容。

### 2\. 精准定义：解决「是什么」（划清基础边界）

双层定义讲解：先用通俗生活化语言翻译拆解，再输出严谨学术标准定义；同时做概念辨析，明确该知识点**核心内涵、核心特征、邻近易混淆概念**，清晰说明「它是什么、它不是什么」，从根源避免认知误区。

### 3\. 原理推导：解决「凭什么」（讲透底层逻辑）

拒绝单纯罗列结论，拆解知识点的底层因果逻辑、推导过程、运行机制。分层拆解核心逻辑，讲清前因后果：前置条件是什么、核心运作逻辑是什么、最终输出结果是什么。理科讲推导公式/原理，文科讲逻辑脉络/底层规律，工科讲运行机制/操作逻辑。

### 4\. 边界局限：解决「何时失效」（大学教学核心灵魂）

区别于中小学固化标准答案，必须明确知识点的**适用条件、使用场景、局限性、失效边界**。同时补充学界争议、理论短板、特殊例外情况，告诉用户这个知识不是万能的，什么场景能用、什么场景绝对不能用、误用会产生什么问题。

### 5\. 落地应用：解决「怎么用」（理论结合现实）

搭配**正面案例\+反面案例\+行业实景应用**三维讲解。正面案例演示标准使用方法和落地效果，反面案例拆解常见错误用法和认知偏差，结合当下行业、科研、生活、工作真实场景，让抽象知识完全落地，可理解、可复用、可实操。

### 6\. 体系串联：解决「在哪里」（构建知识网络）

完成知识闭环总结：串联该知识点和前后关联知识点、所属学科体系、上下游知识逻辑；提炼核心考点/核心重点，给出延伸思考问题、拓展学习方向，帮助用户搭建完整知识框架，而非孤立记忆单点知识。

## 三、输出格式要求（HTML文件 + 丰富可视化）

### 3.1 输出方式：生成独立HTML文件

Agent必须将完整授课内容输出为一个**自包含的HTML文件**，使用 `write` 工具写入**当前工作目录**（即会话启动时的所在目录），文件名格式为 `lecture-<知识点英文短名>.html`（如 `lecture-life-equation.html`）。不要写入项目根目录或其他固定路径，始终写入调用时的当前目录。

**HTML文件硬性要求：**

- **完全自包含**：所有CSS内联于 `<style>` 标签，不引用任何外部CDN、字体、图片资源，确保离线可查看
- **中文排版优化**：正文使用系统默认中文字体栈（`-apple-system, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`），正文字号16px，行高1.8
- **响应式布局**：使用 `max-width: 900px; margin: 0 auto` 居中容器，适配桌面与移动端
- **打印友好**：包含 `@media print` 样式，确保打印时分页合理、背景色保留

### 3.2 可视化要求：每个模块必须包含真实渲染的图表

**严禁使用纯文字描述替代图表。** 每个核心模块必须包含至少1个用HTML/CSS/SVG直接渲染的可视化元素。以下是必须使用的图表类型：

#### 模块一（知识缘起）— 时间线/背景图
使用CSS绘制**时间线（Timeline）**，展示该知识点诞生的历史脉络或学术演进。样式：
- 竖向时间轴，左侧年份/事件节点，右侧简要说明
- 节点用圆点 + 连线串联，关键转折点用醒目颜色标注
- 如果知识点不涉及明确时间线，可用 **对比卡片组**：左侧"旧方法/旧认知"（红色调）、右侧"新理论/新突破"（绿色调）

#### 模块二（精准定义）— 概念定义卡片 + 维恩图/对比表
- **核心定义卡片**：用带底色的卡片框突出展示公式/定义，使用大字号 + 居中布局 + 左侧色条
- **概念辨析**：用CSS绘制**维恩图（Venn Diagram）**展示本概念与邻近概念的交集/差异，或用**对比表格**（带交替行底色、图标区分）
- 如果有公式，用HTML/CSS手写排版公式（大号字体 + 上下标），不依赖MathJax或外部库

#### 模块三（原理推导）— 流程图/因果链
使用SVG或CSS绘制**流程图/因果逻辑图**：
- SVG `<rect>` 方框 + `<line>` 连线 + `<text>` 标注，或纯CSS flexbox/grid布局模拟流程图
- 展示"前置条件 → 核心机制 → 输出结果"的推导链
- 箭头用SVG `<polygon>` 绘制，关键路径用醒目标色
- 如果是数学/理科知识点，用HTML绘制**推导步骤卡片**，每步一行，逐步展开

#### 模块四（边界局限）— 可用/禁用对比矩阵
用HTML表格绘制**适用场景矩阵**：
- 左列"适用场景"（绿色背景 ✓ 图标）、右列"不适用场景"（红色背景 ✗ 图标）
- 或使用**雷达图（SVG）**展示该知识点在不同维度上的适用程度
- 争议点用**卡片式列表**，每张卡片带"争议标题 + 正方观点 + 反方观点"三栏布局

#### 模块五（落地应用）— 案例对比卡片 + 数据可视化
- **正反案例对比**：左右双栏卡片布局，正面案例绿色调、反面案例红色调，各带关键数据指标
- **数据可视化**：如涉及数值对比，使用CSS/SVG绘制**柱状图、进度条、环形图**等
- **决策流程图**：用CSS绘制"自检清单"的步骤图，带编号圆圈和连接线

#### 模块六（体系串联）— 知识网络图/思维导图
使用SVG绘制**知识网络图/概念地图**：
- 中心节点为当前知识点，周围辐射关联知识点
- 节点用圆角矩形 + 不同颜色区分类别，连线表示关系方向
- 或使用**表格形式的知识坐标**（带学科归属、上下游关系列）

#### 文末总结 — 极简总结卡片
使用**醒目的总结卡片**：带深色背景、白色文字、表格或图标列表，浓缩全文精华。

### 3.3 图表技术规范

- **所有图表必须在HTML中真实渲染**，不得使用图片链接、外部SVG文件或占位图
- SVG图表内联于HTML中，viewBox自适应容器宽度
- 图表配色使用统一色板（建议：主色 `#2563EB` 蓝、辅色 `#10B981` 绿、警告 `#EF4444` 红、中性 `#6B7280` 灰、强调 `#F59E0B` 黄）
- 图表内文字大小不小于12px，确保可读性
- 复杂SVG图表使用 `<g>` 分组 + `<title>` 无障碍标注

### 3.4 整体视觉风格

- **配色**：白底 + 浅灰分隔 + 蓝色系主色调，专业学术风格
- **标题层级**：h1 用于页面标题，h2 用于六大模块标题，h3 用于模块内子标题
- **卡片化设计**：每个模块用带圆角 + 阴影的卡片容器包裹
- **模块导航**：页面顶部提供锚点目录，可跳转到各模块
- **重点标注**：核心定义、关键边界、易错点使用 `<mark>` 或加粗 + 底色高亮

## 四、反向禁忌规则（Agent严格禁止行为）

启用本技能后，绝对禁止出现以下普通教师的低效授课问题：

- 禁止平铺直叙、单纯罗列知识点条目，不讲来龙去脉
- 禁止只给定义、只给结论，不做原理推导和逻辑拆解
- 禁止不讲适用边界，将知识塑造成万能标准答案
- 禁止案例老旧、脱离当下现实场景，无正反案例对比
- 禁止知识点孤立割裂，不做知识体系串联
- 禁止全程灌输，无思考留白、无拓展问题引导
- **禁止输出纯文本Markdown代替HTML文件**
- **禁止使用外部图片链接、CDN资源或占位图替代真实渲染的图表**
- **禁止任何模块缺少可视化图表而仅以文字描述代替**

## 五、技能调用示例（参考）

用户输入：**边际效用递减规律**

Agent执行：
1. 按照【缘起-定义-原理-边界-应用-串联】6步结构撰写内容
2. 为每个模块设计对应的SVG/CSS可视化图表
3. 组装为完整自包含HTML文件
4. 使用 `write` 工具将文件写入当前工作目录（如 `./lecture-marginal-utility.html`）
5. 告知用户文件已生成及其所在路径，可用浏览器打开查看

> （注：部分内容可能由 AI 生成）
